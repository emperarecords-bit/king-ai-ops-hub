# Keeps the Next.js `public/` directory present in the repo and the Docker build
# context. The production image copies `/app/public` (Dockerfile), which fails if
# the directory does not exist. Static assets (favicon, images) go here.
